# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
.venv/
venv/
env/

# Secrets / config
# NOTE: transcoder/config.py is now the secret-free pydantic-settings module
# (real values come from .env, which stays ignored), so it IS tracked.
.env
.env.local
.env.*.local
# Auto-generated session-signing key (created beside the DB when SECRET_KEY is unset)
secret_key

# Local Claude Code settings (per-machine permissions; not for sharing)
.claude/settings.local.json

# Runtime state & artifacts
solution/tmp/
solution/out/
solution/log/
log/
tmp/
out/
*.db
*.db-journal
*.db-wal
*.db-shm
*.db-bak
*.sqlite
*.sqlite3
*.migrated
*.bak

# Legacy state files (now in DB)
last_history_timestamp.txt
excluded_episodes.csv
excluded_movies.csv

# Test / coverage artifacts
.coverage
coverage.xml
htmlcov/

# Type checkers / linters
.mypy_cache/
.ruff_cache/
.dmypy.json

# Personal scratch / planning notes (not for sharing)
TODO.local.md

# IDE / editor
.idea/
.vscode/

# OS
Thumbs.db
.DS_Store
*~
*.swp
*.swo

# Frontend
solution/web/node_modules/
solution/web/dist/
solution/web/*.tsbuildinfo
solution/web/vite.config.js
solution/web/vite.config.d.ts

# Windows installer build artifacts (deploy/h265-transcoder/)
deploy/h265-transcoder/build/
deploy/h265-transcoder/dist/
deploy/h265-transcoder/*.spec
deploy/h265-transcoder/app.ico
